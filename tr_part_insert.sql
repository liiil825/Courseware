BEGIN
    declare @temp table(id int, title nvarchar(100), lessonyear smallint, lessonId int, classNum int, relateId int, relateNum int, rnd int, pathHtml varchar(100), marktime float, isold bit, test tinyint)

END 
